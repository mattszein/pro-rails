<div class="container mx-auto px-4 py-8 max-w-7xl">
  <div class="flex justify-between items-center pb-2">
    <%= link_to "Back to Tickets", settings_tickets_path, class: "text-indigo-500" %>
  <div class="flex gap-2 items-center px-4">
    <%= render(Core::LinkComponent.new(name: "Edit", url: edit_settings_ticket_path(@ticket), options: {data: {turbo_frame: "modal"}, custom_style: {style: :as_button, theme: :edit, size: :sm}})) %>

  </div>
  </div>

  <%= turbo_stream_from dom_id @ticket %>
  <%= render @ticket %>
</div>
