<div class="w-full">
  <div class="flex justify-between items-center pb-2">
    <h1 class="text-xl font-semibold"><%= t("adminit.navigation.accounts") %></h1>
  </div>
  <%= render Core::CardComponent.new do %>
    <%= render Core::TableComponent.new(
          rows: @accounts,
          columns: accounts_columns,
          options: {
            frame_id: "admin_accounts_table",
            pagy: @pagy,
            sortable: true,
            filterable: true
          }
        ) %>
  <% end %>
</div>
