<div class="w-full">
  <div class="flex justify-between items-center pb-2">
    <h1 class="text-xl font-semibold"><%= t("adminit.navigation.roles") %></h1>
  </div>

  <%= render Core::CardComponent.new do %>
    <div class="flex p-4 gap-4">
      <%= render Core::TableComponent.new(rows: @roles, columns: roles_columns) %>
    </div>
  <% end %>
</div>
