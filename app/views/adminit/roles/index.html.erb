<div class="w-full">
  <div class="flex justify-between items-center pb-2">
    <h1 class="text-xl font-semibold"><%= t("adminit.navigation.roles") %></h1>
  </div>

  <%= render Core::CardComponent.new do %>
    <%= render Core::TableComponent.new(
          rows: @roles,
          columns: roles_columns,
          options: {
            frame_id: "admin_roles_table",
            pagy: @pagy
          }
        ) %>
  <% end %>
</div>
