<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Pro Rails" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= action_cable_with_jwt_meta_tag(current_user: current_account) if current_account %>
    <%= javascript_importmap_tags %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Plus+Jakarta+Sans:wght@400;600;700&family=Space+Grotesk:wght@400;600;700&family=Outfit:wght@400;600;700&family=Bricolage+Grotesque:wght@400;600;700&family=Google+Sans+Code:wght@400;600;700&family=Orbitron:wght@400;600;700;900&family=Manrope:wght@400;600;700&display=swap" rel="stylesheet">
    <% if current_account %>
      <meta name="current-user-id" content="<%= current_account.id %>">
    <% end %>
  </head>
  <body class="antialiased bg-default" data-controller="closer">
    <%= render(Core::SidebarComponent.new(open: false, title: "RailsApp | Menu")) do %>
      <%= render "partials/navigation" %>
    <% end %>
    <main class="flex-grow pt-16 px-8 overflow-x-scroll" role="main">
      <%= turbo_frame_tag "modal", target: "_top" %>
      <%= yield %>
    </main>
    <%= render "shared/flashes" %>

<div data-controller="notifications" 
     class="fixed bottom-0 right-0 z-50 p-4 w-full max-w-sm pointer-events-none space-y-4">
  
  <div data-notifications-target="container" class="flex flex-col gap-2"></div>

  <template data-notifications-target="template">
    <div class="pointer-events-auto w-full max-w-sm overflow-hidden rounded-lg bg-white shadow-lg ring-1 ring-black ring-opacity-5 transform transition-all duration-300 ease-out translate-y-2 opacity-0 scale-95"
         data-role="toast">
      <div class="p-4">
        <div class="flex items-start">
          
          <div class="flex-shrink-0">
            <svg class="h-6 w-6 text-green-400" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>

          <div class="ml-3 w-0 flex-1 pt-0.5">
            <p class="text-sm font-medium text-gray-900" data-role="title"></p>
            <p class="mt-1 text-sm text-gray-500" data-role="message"></p>
          </div>

          <div class="ml-4 flex flex-shrink-0">
            <button type="button" data-action="click->notifications#close" class="inline-flex rounded-md bg-white text-gray-400 hover:text-gray-500 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
              <span class="sr-only">Close</span>
              <svg class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                <path d="M6.28 5.22a.75.75 0 00-1.06 1.06L8.94 10l-3.72 3.72a.75.75 0 101.06 1.06L10 11.06l3.72 3.72a.75.75 0 101.06-1.06L11.06 10l3.72-3.72a.75.75 0 00-1.06-1.06L10 8.94 6.28 5.22z" />
              </svg>
            </button>
          </div>

        </div>
      </div>
    </div>
  </template>
</div>

  </body>
</html>
