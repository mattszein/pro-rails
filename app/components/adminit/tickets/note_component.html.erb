<div class="p-3 rounded-lg <%= note_classes %>">
  <div class="flex justify-between items-start mb-1">
    <span class="text-xs font-bold uppercase tracking-wider <%= label_classes %>">
      <% if note.system? %>
        <%= t("views.notes.system") %><%= " • by #{note.account.email}" if note.account %>
      <% else %>
        <%= t("views.notes.note_by") %> <%= note.account&.email || t("views.notes.unknown") %>
      <% end %>
    </span>
    <span class="text-xs text-slate-400 text-nowrap ml-2"><%= time_ago_in_words(note.created_at) %> <%= t("views.notes.ago") %></span>
  </div>
  <p class="text-sm text-slate-700 dark:text-slate-300"><%= note.body %></p>
</div>
