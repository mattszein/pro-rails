<%= render(Core::LinkComponent.new(url: path.is_a?(Symbol) ? send(path) : path, options: link_options)) do %>
  <%= helpers.icon icon_name, {classes: icon_classes} %>
  <span class="<%= span_classes %>"><%= label %></span>
<% end %>
