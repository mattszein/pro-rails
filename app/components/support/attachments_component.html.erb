<div class="flex justify-between items-center">
  <h3><%= t("support.attachments.title") %></h3>
</div>
<% if has_attachments? %>
  <div class="grid grid-cols-2 md:grid-cols-3 gap-4">
    <% attachments.each do |attachment| %>
      <%= render Support::AttachmentItemComponent.new(attachment: attachment) %>
    <% end %>
  </div>
<% else %>
  <p class="text-gray-500 text-sm">
    <% if show_upload_form %>
      <%= t("support.attachments.no_attachments_upload") %>
    <% else %>
      <%= t("support.attachments.no_attachments") %>
    <% end %>
  </p>
<% end %>
<% if show_upload_form %>
  <%= render Support::AttachmentUploadFormComponent.new(
      ticket: ticket
    ) %>
<% end %>
