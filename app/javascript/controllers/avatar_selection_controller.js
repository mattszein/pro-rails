import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  // Handles avatar accept/reject/regenerate interactions
  // The actual actions are handled by form submissions to the server
}
